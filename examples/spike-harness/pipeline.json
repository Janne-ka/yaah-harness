{
  "nodes": {
    "loop": {
      "type": "agent_loop",
      "backend": "fake_tool",
      "max_turns": 5,
      "system_prompt": "file:agent",
      "tools": {
        "read_file": {
          "description": "Read a file at the given path. Returns the file's text contents.",
          "input_schema": {"type": "object", "properties": {"path": {"type": "string"}}, "required": ["path"]},
          "dispatch": "fn:tools:read_file"
        },
        "done": {
          "description": "Signal that the task is complete. Provide a summary of what was learned.",
          "input_schema": {"type": "object", "properties": {"summary": {"type": "string"}}, "required": ["summary"]},
          "dispatch": "fn:tools:done"
        }
      }
    }
  },
  "graph": {
    "start": "loop",
    "stages": {
      "loop": {"node": "loop", "then": null}
    }
  }
}
