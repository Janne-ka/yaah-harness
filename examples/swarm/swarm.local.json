{
  "transport": {"type": "inproc"},
  "providers": {"fake": {"type": "fake_scripted", "by_model": {
    "extract": ["{\"requirements\": [\"auth requires MFA\", \"logs kept 90 days\", \"data encrypted at rest\"]}"],
    "skeptic": ["Refuted: MFA scope excludes service accounts.",
                "Holds: retention is explicit in §4.",
                "Refuted: encryption covers primary store only."]
  }}},
  "default_provider": "fake",
  "state": {"type": "memory"},
  "pipeline": "swarm.json",
  "input": "input.json",
  "run": true
}
