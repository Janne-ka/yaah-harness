# run artifacts — regenerated by every run; don't track
.arch-drift-state*/
arch-report*.html
__pycache__/

# UTC-timestamped versioned diagram outputs — pile up over time; commit
# only the `-a.svg` / `-b.svg` / `latest.svg` pointers next to them
diagrams/*[0-9]Z.svg
