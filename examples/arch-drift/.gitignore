# run artifacts — regenerated by every run; don't track
.arch-drift-state*/
arch-report*.html
__pycache__/

# diagram outputs — fully regenerable; the pipeline IS what we commit, not its output
diagrams/
