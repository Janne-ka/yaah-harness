{
  "_doc": "Real-model config for the config-flow visualizer. Extends local, swaps the fake provider for the real claude_cli backend. Gateless — config-flow is regenerable, so it runs end-to-end with no human gate (the `visualize` wrapper extends this directly).",
  "_extends": "config-flow.local.json",
  "providers": {
    "claude": {
      "type": "claude_cli",
      "binary": "claude",
      "by_model": null
    }
  }
}
