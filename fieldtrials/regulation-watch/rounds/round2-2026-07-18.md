# Round 2 — 2026-07-18

Run against the post-round-1 engine (with `gate-route-not-in-form`) and the
hardened kit (non-editable wheel install, out-of-bounds brief, real L2). All
seven verified claims were code-checked before any fix (two builders agreeing is
not proof).

## Matrix

| Run | Model | Part | End-to-end | Isolation |
|-----|-------|------|-----------|-----------|
| 1 | opus | A (author) | green | clean (no repo/answer-key reads) |
| 2 | opus | B (debug) | green | clean |
| 3 | sonnet | A (author) | green | clean |
| 4 | sonnet | B (debug) | green | clean |

## Headline results

**Isolation fixed.** Round 1's biggest integrity flaw (a builder read
`answer-key.md` via the editable install) did not recur. The wheel install put
`yaah` under site-packages; zero repo-tree or answer-key reads by anyone. Two
minor elevated flags (one empty attempted site-packages read; one shallow
site-packages grep, self-annotated "not reading engine src per rules") — neither
touched a trap's answer.

**L3 — the clearest win.** Both B builders fixed it in one attempt AND both chose
the semantically-correct fix (`approve_or_revise` + rename `reject`→`revise`,
preserving loop-back) over the lazy route-drop. The `gate-route-not-in-form` lint
added after round 1 works, and the floor tier (sonnet) matched the ceiling on
contract reasoning — a direct payoff of the round-1 fix.

**L1 / L4 / L5 stable.** 6/6 rescued in one attempt (lint / `--json`), identical
surfacing order both runs.

**L2 — found still mis-calibrated (second round).** Both builders correctly read
the `{{?judge_notes}}` decoy as "dead but harmless": the adjacent live
`{{loop_feedback}}` line (once L4 is fixed) carries the judge's notes, so the loop
is not blind. The re-seed measured nothing real. Fixed this round — see below.

## Part A friction convergence (both builders, independently)

1. **Transform return nests under `result`, not flat** — killed the manual's own
   cursor-loop recipe. opus ~30min, sonnet ~1.5hr (core detour). Both rebuilt on
   agent-driven cursors. → the recipe was broken as written (infinite-loops).
2. **Agent node in `validators:[]` fails opaquely** ("no failure detail"). ~20-30
   min each; both pivoted to a branch back-edge loop.
3. Transform key/signature confusion (`target` vs `call`; one-arg fn); bogus
   config passes `validate --strict`, dies at runtime.

## Verified findings → actions

| Finding | Verdict | Action |
|---------|---------|--------|
| Manual cursor-loop recipe infinite-loops (this-wave regression) | CONFIRMED | Fixed: teach `call:"envelope"` cursor; recipe verified to run |
| 7 scaffold docs still cite non-shipping `docs/*` (round-1 fold incomplete) | CONFIRMED | Fixed: repointed to `yaah manual` / marked source-repo-only |
| Transform calling conventions undocumented; docstring says "spread" but code replaces | CONFIRMED | Fixed: manual section + docstring corrected |
| L2 re-seed still defeated by the live `loop_feedback` line | CONFIRMED | Fixed: merged L2+L4 into one layered trap (4 traps) |
| Agent-in-`validators` fails with empty failures / "no failure detail" | CONFIRMED | Fixed: runtime `validator_not_a_checker` structured failure |
| Bogus transform `target` passes `--strict` | CONFIRMED | DEFERRED (trigger: repeat incident; node:role-only breaks sibling consistency) |
| `fake_scripted` content-cursor skips entries (opus-A F7) | CONFIRMED | KILLED as a new diagnostic (fires on every correct resume; already documented) — mitigated by the recipe fix (envelope cursor consumes no scripted entries) |
| "loop-key-mismatch" agent-prompt lint (scorer's hoped-for L2 tooling catch) | — | KILLED: unsound + redundant (`carry_gap` covers the sound subset; agent-prompt reads substitute empty, no fail-loud consequence) |

## Kit change: L2 is now a layered L4 trap

The draft prompt reads a single bare `{{judge_notes}}` line. STATE 1: faults
`render_unfilled_placeholders` (loud). STATE 2: builder adds `?` → clears the
fault but the loop now runs provably blind (judge feedback never reaches the
re-draft), no lint, no fault. STATE 3 (full fix): repoint to `{{?loop_feedback}}`.
The offline overlay now forces one revise cycle so the loop actually executes.
Trap count: 5 → 4 (four sound beats five with one broken). `selfcheck.py` asserts
all three states.

## Round-over-round delta

| Dimension | R1 | R2 |
|-----------|----|----|
| L3 | shipped broken by both | both fixed, both chose the correct fix ▲▲ |
| Isolation | answer-key read (editable install) | zero out-of-bounds reads ▲▲ |
| Executor vs ceiling | matched trap outcomes | sonnet matched opus on L3 fix *quality* ▲ |
| L2 | vestigial | still weak → re-seeded as layered trap this round ◆ |
| New surface | — | Part A exposed the transform-nesting + validator-opacity walls |

## Baseline for round 3

- Traps: L1 (missing-carry), L3 (gate-route lint, first signal), L4-layered
  (loud fault → silent blind loop → full fix), L5 (schema_mismatch).
- The layered L4 is the new acid test: does the builder stop at the `?` fix
  (silent blind loop shipped) or trace the feedback to `loop_feedback`?
- Part A now has a buildable cursor-loop recipe and a loud validator-misuse
  error — round 3 measures whether those two walls are gone.
