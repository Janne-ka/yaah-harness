# Part-B fixtures

A copy of the shared notice fixtures from `../../fixtures/notices/`, so Part B
is a self-contained project. The offline overlay (`root.local.json`) feeds a
single notice via `input.json`; these files are here for the builder to expand
the pipeline to a batch/foreach if they choose.
