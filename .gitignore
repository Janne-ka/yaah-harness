# OS
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# pixi — local solved environments (manifest + pixi.lock are committed)
.pixi/

# build artifacts
/build/
dist/

# coverage
.coverage
.coverage.*
htmlcov/
coverage.xml

# local planning / working notes (not part of the repo)
.notes/

# local-only (never published): personal Claude Code settings + the private
# banlist of a consuming app's proper nouns (see docs/contributor/banlist.txt)
.claude/settings.local.json
docs/contributor/banlist.local.txt

# internal dogfood (opposer pipeline + its output) — not shipped
.opposer/
